logger:
  format: text
  level: debug

postgres:
  max_conns: 10
  max_lifetime: 20s

inventory_service:
  host: inventory-service
  port: 50051

server:
  host: 0.0.0.0
  port: 8080
  read_timeout: 15s
  write_timeout: 15s
  idle_timeout: 60s
  shutdown_timeout: 30s